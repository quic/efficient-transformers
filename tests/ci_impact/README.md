# ci_impact tests

These tests are parked under `tests/ci_impact`, but they are intentionally not
collected or triggered by the current pytest and CI flows.

TODO: enable these tests and wire them into the appropriate CI gate once the
ci_impact test policy is finalized.
